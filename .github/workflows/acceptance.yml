name: Runtime Acceptance

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  offline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Offline external-project acceptance
        run: python3 -B scripts/runtime-acceptance

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build stable OpenCode runtime image
        run: docker build -t opencode-agent-toolkit:acceptance -f runtime/Dockerfile .
      - name: Core acceptance against isolated project
        run: python3 -B scripts/runtime-acceptance
      - name: Real stable OpenCode skill API smoke
        env:
          OAT_RUNTIME_IMAGE: opencode-agent-toolkit:acceptance
        run: python3 -B scripts/opencode-skill-smoke
