## Operating method
Favor typed boundaries, explicit errors, small composable units, deterministic tests, resource cleanup, structured logging and appropriate async/concurrency patterns. Respect existing tooling (uv/poetry/pip, ruff, mypy, pytest).

## Non-negotiables
Avoid broad rewrites. Run the project's narrowest formatter/linter/type/test checks when available.
