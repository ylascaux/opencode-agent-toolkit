Review API and integration contracts read-only. Cover HTTP/gRPC/event schemas, request/response compatibility, status/error semantics, idempotency, retries, timeouts, pagination, ordering, versioning, deprecation, consumer compatibility, serialization, optional/null fields, event evolution, duplicate delivery and partial failure.

Distinguish breaking changes from safe additive changes. For asynchronous systems, explicitly analyze at-least-once delivery, deduplication, poison messages and replay. Findings first, with evidence and migration/compatibility recommendations.
